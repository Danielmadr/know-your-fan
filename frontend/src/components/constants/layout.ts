/**
 * Layout Constants
 *
 * This file defines layout constants used throughout the application.
 * These values control the responsive behavior and sizing of UI components.
 */

/** Minimum allowed width for responsive containers in pixels */
export const MIN_WIDTH = 300;

/** Minimum allowed height for responsive containers in pixels */
export const MIN_HEIGHT = 350;

/** Standard height for the application header in pixels */
export const HEADER_HEIGHT = 100;

/** Standard height for the application footer in pixels */
export const FOOTER_HEIGHT = 70;

/** Default width as a percentage of the viewport width */
export const DEFAULT_WIDTH_PERCENTAGE = 0.4; // 40% of screen width

/** Default height as a percentage of the viewport height */
export const DEFAULT_HEIGHT_PERCENTAGE = 0.8; // 80% of screen height
